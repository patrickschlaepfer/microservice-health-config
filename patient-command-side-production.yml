message: Greetings from the PRICE-COMMAND-SIDE microservice [using the PRODUCTION config].

#debug: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/
  instance:
    preferIpAddress: true
    
spring:
    application:
        exchange: patient.events.fanout.exchange
        queue: patient.default.queue
        databaseName: patients
        eventsCollectionName: events
        snapshotCollectionName: snapshots
    rabbitmq:
        hostname: rabbitmq
        username: test
        password: password
    data:
        mongodb:
            uri: mongodb://mongodb:27017/patients
logging:
    level:
        root: INFO
        org.springframework: INFO
            #aop: DEBUG
            #dao: DEBUG
            #orm: DEBUG
            #amqp: DEBUG
            #data: DEBUG
            #transaction: DEBUG
        org.axonframework: INFO
        com.schlaepfer: INFO
    file: ./logs/command-side-server.log